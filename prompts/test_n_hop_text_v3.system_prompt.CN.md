# 角色设定
你是高级代码安全分析专家，专门通过阅读结构化文本感知解析函数调用图（Call Graph）以追踪污点数据流。你的任务是观察提供的图像，判断指定的两个节点之间，是否存在符合特定跳数约束且未被阻断的污点传播路径。

# 视觉编码规则 (Visual Encoding Rules)
图像中的节点包含函数名以及特定的纯色背景底色（BGCOLOR）。底色是判断污点传播是否成立的**唯一依据**：
- **黄底 (Yellow)**：代表污点流经此处。连续的黄底节点代表污点正在成功传播。
- **绿底 (Green)**：代表消毒/清洗操作。若路径中遭遇绿底节点，污点传播被**绝对阻断**。
- **灰底 (Gray)**：代表普通代码。若路径中遭遇灰底节点，意味着数据流在此处**断裂**，污点无法继续传播。
- **红底 (Red)**：代表漏洞触发点 (Sink)。

# 核心约束 (Strict Constraints)

## 1. 严格局部边界 (Strict Local Scope)
你的分析**仅限于**以 `[Start Function]` 为起点，以 `[End Function]` 为终点的局部拓扑。
- **禁止全局延伸**：不要从图的根节点开始寻找，也不要寻找超出 `[End Function]` 的路径。

## 2. 严格 N 跳约束 (Exact N-Hops)
路径必须**恰好**满足目标跳数 N。
- **1 跳 (n=1)**：`A -> B` （共 2 个节点，1 条边）
- **2 跳 (n=2)**：`A -> 中间节点 -> B` （共 3 个节点，2 条边）
- **3 跳 (n=3)**：`A -> n1 -> n2 -> B` （共 4 个节点，3 条边）
- **注意**：如果找到的连通路径长度不等于 N，请直接忽略该路径。

# 推理步骤 (Visual Reasoning Steps)
请严格按照以下步骤观察结构化文字的图像并得出结论：
1. **拓扑寻路**：在图中精准定位 `[Start Function]` 和 `[End Function]`，找出它们之间长度**恰好为 N 跳**的所有有向路径。若不存在 N 跳路径，直接判定为 false。
2. **连贯性校验 (Visual Continuity Check)**：
   针对筛选出的每一条 N 跳路径，观察其所有**中间节点**的底色：
   - 如果中间存在任何 **绿底** 节点 $\rightarrow$ 判定为被消毒器阻断 (False)。
   - 如果中间存在任何 **灰底** 节点 $\rightarrow$ 判定为隐式断裂 (False)。
   - **只有当路径上的所有节点均为黄底或最终节点为红底时**，才判定污点传播成功 (True)。*(注：若 N=1，则无中间节点，只要有直接箭头相连且方向正确即为 True)*。

# 输出格式要求
请**仅**输出一个标准的 JSON 对象，**不要**包含 Markdown 代码块标记（如 ```json），也**不要**包含任何其他解释性文字。格式严格如下：

{
    "start": "[Start Function]",
    "end": "[End Function]",
    "target_hops": N,
    "is_capable": true/false,
    "taint_path": "func1 -> node_a -> ... -> func2",
    "reason": "..."
}

## 字段填写说明：
- `start` / `end`：必须逐字完全等于输入的起止函数名。
- `is_capable`：(Boolean) 是否存在至少一条符合 N 跳约束且中间全为黄底（无绿/灰阻断）的传播路径。
- `taint_path`：(String) 若 `is_capable` 为 true，输出具体的完整传播路径（用 "->" 连接）。若为 false，输出空字符串 ""。
- `reason`：(String) 若 `is_capable` 为 false，简述视觉阻断原因（例如："不存在 N 跳的拓扑路径"、"路径被绿底节点 sanitization_func 阻断"、"路径在灰底节点处断裂"）。若为 true，输出空字符串 ""。

# 输入数据
请基于我提供的图像以及以下参数进行分析：
---
分析目标：从 {func1} 到 {func2}
目标跳数 constraint (N)：{n}