请仔细分析这张函数调用图或流程图，识别出图中所有的节点和有向边。这是一项极其严格的 OCR 和视觉转录测试，请务必直接且准确地提取信息。

【核心纪律：防幻觉、防篡改、防死循环】
1. 严禁“自作聪明”地化简或去重：如果节点名称中包含连续重复的单词或命名空间（例如图上写着 `training.training`），你必须原封不动地全部转录！绝不可擅自将其缩减、合并或省略为 `training`。图上有几个重复词，你就必须写几个，绝对忠于原图。
2. 严禁联想与补全：你必须像一台纯粹的扫描仪一样，只提取图中“肉眼真实可见”的文字。绝不可调用你的编程知识自动补全函数名（例如，绝不可自行脑补 talloc、setfacl 等库的 API 序列）。看不清的字宁可照着像素输出，也绝不瞎编。
3. 精确转录标点：节点上的函数名通常在每个节点的最上方，底色为白色。必须完全保留原有的下划线（_）、点号（.）、大小写、括号（()）以及任何特殊标点符号。
4. 严禁无意义的死循环：提取的节点必须是图上真实存在的不同节点，请自行检查，绝不允许在输出时陷入无脑重复同一个函数名的死循环。

【有向边识别要求】
仔细观察连线和箭头方向，提取所有的跳转关系。必须明确区分“起点”和“终点”。

【输出格式】
请严格输出一个合法的 JSON 对象，不要包含任何注释，不要包含 markdown 代码块之外的任何多余解释文字。请严格遵循以下 JSON 结构和数据类型：

```json
{
    "node_counts": X,  // int类型，节点个数
    "all_nodes":[  // list类型，列举所有节点的函数名的列表
        "exact_function_name_1",
        "exact_function_name_2"
    ],
    "edge_counts": Y,  // int类型，边个数
    "all_edges":[  // list类型，列举所有有向边的有向边列表
        "source_function->target_function"
    ]
}