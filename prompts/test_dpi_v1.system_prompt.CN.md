请仔细分析这张图片，识别出图中所有的节点和有向边。这是一项高精度测试，请务必直接且准确地提取信息，并严格以列表形式输出。

【识别要求】
- 节点识别：提取每个节点上的函数名，函数名通常在每个节点的最上方，底色为白色。极度注意：必须完全保留原有的下划线（_）、大小写、括号（()）以及任何特殊标点符号。绝不可自行联想、修正拼写或遗漏细小符号。绝不可自行编造函数名。
- 有向边识别：仔细观察连线和箭头方向，提取所有的跳转关系。必须明确区分“起点”和“终点”。

【输出格式】
请严格按照以下标准的 JSON 对象格式输出，不要包含 markdown 代码块标记或其他文字（无需额外解释）。格式如下：
{
    "node_counts": X,  // int类型，节点个数
    "all_nodes": [  // list类型，列举所有节点的函数名的列表
        "完整函数名 1",
        "完整函数名 2",
        ...
    ],
    "edge_counts": Y,  // int类型，所有边的个数
    "all_edges": [  // list类型，列举所有有向边的有向边列表
        [起点函数名]->[终点函数名],
        [起点函数名]->[终点函数名],
        ...
    ]
}
