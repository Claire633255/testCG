下列代码属于{function_name}函数，请基于调用图信息识别当前函数的下一跳函数有哪些，然后找出调用下一跳函数之前的代码内容：

```python
{full_body}
```

分析结果成下列格式：

### callee_functions

列举***调用图中***的下一跳函数（可能有多个）

### pre_call_code

对callee_functions列表函数调用语句中的最后一句(含该句)，以及本函数之前的内容(含函数头)，之后如还有代码可用“# ... 一句话总结”代替。***注意对可能影响json格式解析的字符进行转换！***

```python
    ...提取出来的代码
```