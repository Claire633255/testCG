你是一个专业的系统拓扑图分析专家。这是一张函数调用关系图。
我已经通过高精度的机器扫描，确认了图中【绝对存在】且【拼写绝对正确】的节点列表。

【已知真实的节点列表】（请严格信任并只使用这些节点名）：[all_node_list]

【你的任务：提取有向边】
请仔细观察图片中的连线和箭头方向，找出上述已知节点之间的所有跳转关系。
严禁你在已知列表之外自行编造任何新的节点名！即使你觉得列表里的名字（如连续的 training）有拼写错误，也必须原封不动地使用我提供的名字作为起点或终点。

【输出格式】
请严格输出一个合法的 JSON 对象，不要包含 markdown 代码块之外的多余文字。格式如下：

```json
{
    "edge_counts": Y,
    "all_edges":[
        "start_function->end_function",
    ]
}
```